# ═══════════════════════════════════════════════════════════════
# 环境变量配置示例
# ═══════════════════════════════════════════════════════════════
# 使用说明：
#   1. 复制此文件为 .env
#   2. 填入你的 API Key
#   3. 运行 `python main.py`

# OpenAI API Key（必需）
OPENAI_API_KEY=your_api_key_here

# OpenAI Model Name (Optional, default: gpt-4o)
OPENAI_MODEL_NAME=gpt-4o

# OpenAI Base URL (Optional, e.g. for proxies or compatible APIs)
OPENAI_BASE_URL=https://api.openai.com/v1

# ═══════════════════════════════════════════════════════════════
# 对话记忆管理（防止上下文溢出）
# ═══════════════════════════════════════════════════════════════
# 当对话超过 token 限制时，自动总结旧消息以节省上下文

# 总结模型（用于压缩对话历史，默认：gpt-4o-mini）
# 建议使用便宜的模型，因为总结不需要高智能
# SUMMARIZATION_MODEL=gpt-4o-mini

# 触发总结的 token 数（默认：4000）
# 当对话超过此值时自动触发总结
# 根据你的主模型上下文大小调整（如 gpt-4o: 128k, gpt-4o-mini: 128k）
# SUMMARIZATION_TRIGGER_TOKENS=4000

# 保留的最近消息数（默认：20）
# 总结后保留最近 N 条原始消息，其余被总结压缩
# SUMMARIZATION_KEEP_MESSAGES=20

# ═══════════════════════════════════════════════════════════════
# 其他配置
# ═══════════════════════════════════════════════════════════════

# LangSmith 追踪（可选,用于调试）
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=your_langsmith_key

# Brave Search API Key（用于 web_search 工具）
BRAVE_API_KEY=your_key_here